type arrayDeStrings = [string, string, string];

const tripulantes: arrayDeStrings[] = [
    ['um', 'dois', 'tres'],
    ['um', 'dois', 'tres'],
    ['um', 'dois', 'tres'],
    ['um', 'dois', 'tres']
]

const array: arrayDeStrings = [
    '1', '2', '3'
]

